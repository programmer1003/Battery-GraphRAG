# ==========================================
# 🔋 Battery-GraphRAG 项目核心依赖清单
# ==========================================

# ------------------------------------------
# 1. 大模型与接口通信
# ------------------------------------------
openai>=1.10.0
httpx>=0.27.0      # 新增：Agent 异步呼叫算法引擎使用

# ------------------------------------------
# 2. 图数据库与向量引擎 (Hybrid RAG)
# ------------------------------------------
neo4j>=5.17.0
sentence-transformers>=2.5.0  # 新增：加载 BGE 向量模型进行语义匹配

# ------------------------------------------
# 3. 环境变量安全读取
# ------------------------------------------
python-dotenv>=1.0.0

# ------------------------------------------
# 4. 非结构化文档解析 (Text2Graph 管道)
# ------------------------------------------
PyPDF2>=3.0.0

# ------------------------------------------
# 5. 微服务后端引擎
# ------------------------------------------
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.6.0

# ------------------------------------------
# 6. 前端可视化 UI
# ------------------------------------------
streamlit>=1.32.0
requests>=2.31.0   # 新增：Streamlit 接收 SSE 流式数据打字机特效

# ------------------------------------------
# 7. 算法引擎与数据处理 (TF-GDC 时序预测)
# ------------------------------------------
pandas>=2.0.0      # 新增：处理 B008.csv 电池时序数据
numpy>=1.24.0      # 新增：矩阵运算与 Z-score 标准化
# torch>=2.0.0     # ⚠️ 注意：本地开发取消注释即可。如果使用 Docker 部署，请将其保持注释，让 Dockerfile 单独安装轻量级 CPU 版